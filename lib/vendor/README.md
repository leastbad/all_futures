# Vendored Active Entity
#
# Active Entity (https://github.com/jasl/activeentity) is abandoned upstream.
# The library is vendored here under MIT (see ACTIVE_ENTITY_MIT-LICENSE) so
# All Futures can ship without a path: or rubygems dependency on a dead gem.
#
# Source of this tree: leastbad/activeentity @ rails-8-compat (mutex_m removed
# for Ruby 3.4, Rails dependency cap lifted to < 9).
